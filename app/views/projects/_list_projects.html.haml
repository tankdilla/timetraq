%ul
  - projects.each do |project|
    %li
      = link_to project.name, [project.user, project]
      %ul
        - project.goals.in_progress.each do |goal|
          %li 
            = link_to goal.name, [project.user, goal]